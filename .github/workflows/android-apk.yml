name: Build Android APK

on:
  push:
    branches: ["main", "master"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Create clean Flutter app workspace
        working-directory: .
        run: |
          rm -rf __build_app
          flutter create -t app --platforms=android --org=com.softism.botmanager --project-name=softism_bot_manager __build_app
          cp pubspec.yaml __build_app/pubspec.yaml
          rm -rf __build_app/lib
          cp -R lib __build_app/lib

      - name: Debug: show workspace
        shell: bash
        working-directory: .
        run: |
          echo "ROOT PWD=$(pwd)"
          ls -la
          echo "--- __build_app exists? ---"
          ls -la __build_app || true
          echo "--- __build_app/android ---"
          ls -la __build_app/android || true
          echo "--- flutter ---"
          flutter --version
          flutter doctor -v || true

      - name: Flutter Pub Get
        shell: bash
        working-directory: __build_app
        run: flutter pub get

      - name: Build Debug APK
        shell: bash
        working-directory: __build_app
        run: |
          pwd
          ls -la
          ls -la android
          flutter build apk --debug -v

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: __build_app/build/app/outputs/flutter-apk/app-debug.apk

